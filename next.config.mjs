/** @type {import('next').NextConfig} */
const nextConfig = {reactStrictMode: true,
    images: {
      domains: ['nozgmmcdmegiovznmcnz.supabase.co'], // Replace with your Supabase project domain
    },};

export default nextConfig;
